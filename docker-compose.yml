version: "3.9"

services:
  app:
    build: .
    container_name: url_shortener_app
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      MONGO_URI: mongodb+srv://avisek338:avisek123@cluster0.wzynilg.mongodb.net/URL_SHORTNER?retryWrites=true&w=majority&appName=Cluster0
      BASE_URL: localhost:8080
      REDIS_PASSWORD: LrhPWu4hX2Gi3XGbVEb4nXTiPOZXYYB2
      ENCODING_SECRET: 0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      - redis

  redis:
    image: redis:alpine
    container_name: redis
    command: ["redis-server", "--requirepass", "LrhPWu4hX2Gi3XGbVEb4nXTiPOZXYYB2"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
